<nav class="flex items-center justify-between flex-wrap bg-teal-500 p-6">
    <div class="flex items-center flex-shrink-0 text-white mr-6">

        <span class="font-semibold text-xl tracking-tight">Fakebook</span>
    </div>
    <div class="block lg:hidden">
        <button class="flex items-center px-3 py-2 border rounded text-teal-200 border-teal-400 hover:text-white hover:border-white">
            <svg class="fill-current h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
        </button>
    </div>
    <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
        <div class="text-sm lg:flex-grow">



            {% if app.user and app.user.profile %}
                <a href="{{ path('app_profile', {id: app.user.profile.id}) }}" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4">Profile</a>
                <a href="{{ path('app_notification') }}" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4">notifs</a>
                <a href="{{ path('app_people') }}" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4">Amis</a>
            {% endif %}


            {% if app.user %}
                <a href="{{ path('app_post') }}" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4">Accueil</a>
                <a href="{{ path('app_post_create') }}" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4">Create</a>


                <a href="{{ path('app_logout') }}" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white">Logout</a>
                {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
                    <a href="{{ path('app_admin') }}" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white">Admin</a>
                {% endif %}
            {% else %}
                <a href="{{ path('app_register') }}" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4">Register</a>
                <a href="{{ path('app_login') }}" class="block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4">Login</a>
            {% endif %}


        </div>
    </div>
</nav>